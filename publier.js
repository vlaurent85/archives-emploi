const submitForm=document.getElementById("submitForm"),submitStatus=document.getElementById("submitStatus"),submitOfferButton=document.getElementById("submitOfferButton");async function sendProposal(event){event.preventDefault();submitOfferButton.disabled=true;submitOfferButton.textContent="Envoi…";submitStatus.textContent="";submitStatus.className="form-status";const form=new FormData(submitForm);const value=name=>{const v=String(form.get(name)||"").trim();return v===""?null:v};const payload={titre:value("titre"),employeur:value("employeur"),ville:value("ville"),departement:value("departement"),region:value("region"),type_contrat:value("type_contrat"),statut:value("statut"),secteur:value("secteur"),categorie:value("categorie"),specialite:value("specialite"),teletravail:value("teletravail"),salaire:value("salaire"),date_limite:value("date_limite"),url_source:value("url_source"),email_contact:value("email_contact"),commentaire:value("commentaire"),statut_validation:"en_attente"};try{const response=await fetch(`${SUPABASE_URL}/rest/v1/propositions`,{method:"POST",headers:{apikey:SUPABASE_KEY,"Content-Type":"application/json","Prefer":"return=minimal"},body:JSON.stringify(payload)});if(!response.ok){const message=await response.text();throw new Error(message||`Erreur ${response.status}`)}submitForm.reset();submitStatus.textContent="Offre envoyée. Elle sera vérifiée avant publication.";submitStatus.className="form-status success"}catch(error){console.error(error);submitStatus.textContent="L’envoi a échoué. Vérifie la configuration Supabase.";submitStatus.className="form-status error"}finally{submitOfferButton.disabled=false;submitOfferButton.textContent="Envoyer l’offre"}}submitForm.addEventListener("submit",sendProposal);